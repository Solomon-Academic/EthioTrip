# Database (configure in backend/Config/database.php or add vars here if extended)

# Gmail SMTP — used for payment/booking notifications (free for development)
# See docs/EMAIL_SETUP.md for App Password instructions
GMAIL_USER=your.email@gmail.com
GMAIL_APP_PASSWORD=your_16_char_app_password
MAIL_FROM_NAME=EthioTrip
MAIL_FROM_EMAIL=your.email@gmail.com

# Optional: legacy Resend keys (no longer used — remove from .env)
# RESEND_API_KEY=
# RESEND_FROM=
